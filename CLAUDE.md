# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Note: `README_RU.md` contains the project narrative (purpose, status) in Russian. This file is the terse operational reference.

## Commands

Run everything from the repo root using the project venv (no system Python):

```powershell
# install deps
.venv\Scripts\python.exe -m pip install -r requirements.txt

# validate config only, no collectors started
.venv\Scripts\python.exe -B scripts\collector.py --dry-run

# collect from all sources (LinkedIn, Company Careers, Djinni, RemoteOK — WTTJ is excluded from "all", see below)
.venv\Scripts\python.exe -B scripts\collector.py --source all --limit 50

# collect from one source, headless
.venv\Scripts\python.exe -B scripts\collector.py --source company_careers --limit 50 --headless

# collect one exact configured query (must match config/job_queries.yaml)
.venv\Scripts\python.exe -B scripts\collector.py --source company_careers --query "Trading Analyst" --limit 20 --headless

# recheck saved URLs and update availability_status
.venv\Scripts\python.exe -B scripts\collector.py --check-availability --check-limit 500 --headless

# open browser for manual WTTJ login, saved into the persistent profile
.venv\Scripts\python.exe -B scripts\collector.py --login

# run the rule-based analyzer over the processed dataset
.venv\Scripts\python.exe -B scripts\analyzer.py

# print/save a skill-gap and rejection-reason report from manual_review + jobs_master
.venv\Scripts\python.exe -B scripts\skills_gap_report.py

# count how often tools and certifications appear in the collected postings (read-only, writes reports/)
.venv\Scripts\python.exe -B scripts\certification_analysis.py

# print/save the list of still-active near-fit postings worth applying to now
.venv\Scripts\python.exe -B scripts\active_near_fit_report.py

# recheck previously-rejected "board exists, 0 jobs" companies (Kraken, Prodly) for new postings
.venv\Scripts\python.exe -B scripts\recheck_rejected_boards.py

# mine a Notion-embed table (e.g. a curated company list) into a CSV via its "Load more" pagination
.venv\Scripts\python.exe -B scripts\mine_notion_companies.py --headless

# score undecided postings for candidate fit via an LLM call (costs money; needs $env:ANTHROPIC_API_KEY)
.venv\Scripts\python.exe -B scripts\fit_score.py --limit 20

# draft CV bullets + a short pitch for "Подходит" + active postings (costs money; needs $env:ANTHROPIC_API_KEY)
.venv\Scripts\python.exe -B scripts\cv_draft.py --limit 10

# review existing CV/drafts/*.md for honesty/relevance/tone via a separate LLM call (costs money; needs $env:ANTHROPIC_API_KEY)
.venv\Scripts\python.exe -B scripts\cv_review.py --limit 10

# assemble a cover-note PDF per draft + ATS keyword check (free, no API key needed)
.venv\Scripts\python.exe -B scripts\cv_pdf.py --limit 20

# refresh the applications tracking sheet from CV/drafts/*.md (free, no API key needed)
.venv\Scripts\python.exe -B scripts\applications_tracker.py

# run all tests
.venv\Scripts\python.exe -B -m unittest discover -s tests

# run one test module / class / method
.venv\Scripts\python.exe -B -m unittest tests.test_ids_and_deduplication
.venv\Scripts\python.exe -B -m unittest tests.test_ids_and_deduplication.JobIdentityTests.test_tracking_parameters_do_not_change_normalized_url
```

LinkedIn and WTTJ collection opens a real (or headless) Chromium via Playwright with a persistent profile at `.browser_profile/`; without `--headless` a visible browser window is expected. `--login` must be run once (or re-run when the session expires) before WTTJ collection works.

## Architecture

Pipeline: `SEARCH → COLLECT → DEDUPLICATE → SAVE RAW` per run, then merge into the accumulating processed dataset. A separate pass: `LOAD PROCESSED → CLASSIFY → EXTRACT SKILLS → SAVE PROCESSED` (the analyzer).

**`core/models.py`** — single `JobRecord` dataclass every collector returns. Nearly all fields are plain strings (sources give incomplete/inconsistent data; normalization happens later, not at parse time). `JOB_RECORD_COLUMNS` derived from the dataclass defines the canonical export column order. Field groups: identity (`job_id`, `source`, `url`), listing (`title`, `company`, `full_text`), geography (`city_region` as originally collected, plus `city`/`region` split out from it and `country`), terms (`work_format`, `source_work_format`, `contract_type`, `salary`, plus `salary_usd_equivalent`), provenance (`date_collected`, `date_published`, `search_query`, `note`), state (`status`, `availability_status`), analysis (`job_category`, `role_family`, `role_subcategory`, `seniority`, `years_experience_min/max`, `skills_all`, `required_skills`, `preferred_skills`, `analysis_note`, plus `fit_score`/`fit_reasoning`). `city`/`region`/`salary_usd_equivalent` are collector-empty — filled by the analyzer pass, not at parse time (see `core/analyzer.py`/`core/metadata.py` below). `fit_score`/`fit_reasoning` are collector- and analyzer-empty too — filled only by the explicitly-invoked, billed `scripts/fit_score.py` (see `core/fit_score.py` below).

**`core/config.py`** — loads the three YAML files under `config/` (`job_queries.yaml`, `companies.yaml`, `settings.yaml`) via `load_configuration()`. `select_queries()` flattens `job_queries.yaml` categories into a flat `[{query, category}]` list; `--query` filters to one exact match (case-insensitive).

**`collectors/base.py`** — `BaseCollector.collect(context, queries, limit) -> CollectorResult` is the contract every adapter implements (async, takes a shared Playwright `BrowserContext`). `setting()` reads nested settings dicts without a config class. All three adapters run inside one shared persistent browser context per `scripts/collector.py:collect_sources`.

Adapters, each in `collectors/`:
- **`linkedin.py`** — searches `/jobs/search/`, collects `/jobs/view/` links, parses JSON-LD `JobPosting` first with CSS-selector fallback. Detects `active`/`closed`/`unknown`. LinkedIn has no stable public API — authwalls and HTML drift are expected failure modes, not bugs to "fix".
- **`wttj.py`** — paginates a fixed topic page (`/fr/pages/emploi-data-analyst?query=...&page=N`), same JSON-LD-first parsing. **The topic page does not reliably honor the query filter** — treat WTTJ as reliable for Data Analyst but not for Crypto/Trading/Risk/Quant; `search_query` records intent, not proof of relevance. Location (`locality`/`region`/country) is merged into one `city_region` string; there is no separate `city` field yet. **Excluded from `--source all`** (`scripts/collector.py:collect_sources`) since 2026-09-04: this topic page is the only one reachable without a login, and it is a French-market page — sampling raw (pre-filter) results confirmed every posting is either French-language (correctly rejected by `requires_french`) or Hybrid (correctly rejected by `has_disallowed_work_format`). The exclusion filters aren't malfunctioning; this specific source can't produce a match for a remote-only, non-French-speaking profile. Still runnable explicitly via `--source wttj` if the profile requirements change or a better public entry point is found.
- **`company_careers.py`** — pulls Greenhouse (`boards-api.greenhouse.io/v1/boards/{slug}/jobs?content=true`) and Lever (`api.lever.co/v0/postings/{slug}?mode=json`) boards that are explicitly listed in `config/companies.yaml`. A "priority company" without a board entry there is silently not collected. Local text filtering by query phrase/significant words. Lever boards can set an optional `host` (e.g. Kaiko is EU-hosted at `api.eu.lever.co`, not the default `api.lever.co` — the default host 404s). A `slug` can point to a shared multi-brand board (Cumberland's crypto roles live on DRW's single Greenhouse board, slug `drweng`, alongside ~150 unrelated DRW postings) — the existing per-title query match still filters those correctly.
- **`djinni.py`** — Ukraine's largest IT job board, added because remote-first EU/US roles keep hard-blocking on residency/visa while Ukraine-market roles don't. RSS-based (`djinni.co/jobs/rss/?primary_keyword=...`), no browser page rendering needed — same "plain HTTP GET" reliability as the Greenhouse/Lever adapters above, not the fragile Playwright-scraping path. Own query vocabulary in `job_queries.yaml`'s `djinni_categories` (Djinni's English UI category labels, e.g. `"Data Analytics"` — NOT the same phrases as the shared `categories` list), since postings are in Ukrainian/Russian and the shared English queries wouldn't match. **An unrecognized `primary_keyword` silently falls back to Djinni's unfiltered all-jobs feed instead of erroring** — same trap as WTTJ's topic page; only add a category value after manually confirming it filters at `https://djinni.co/jobs/?primary_keyword=<value>`. Company name is a heuristic (first `<strong>` tag in the description HTML), not a structured field. Not in `CONTEXT_FILTERED_SOURCES` — Djinni's `primary_keyword` filter is itself the relevance check, and title text is Ukrainian/Russian so the English `query_matches` title check would wrongly reject legitimate results.
- **`remoteok.py`** — remote-only tech job board, added because it's remote-by-construction (no work-format guessing needed — `work_format` is hardcoded `"Remote"`). Plain HTTP GET against `remoteok.com/api`, JSON, no browser rendering. **The documented `?tag=` query parameter does NOT filter server-side** — passing it returns zero postings (verified 2026-09-06), so the collector fetches the unfiltered feed and applies the same local `query_matches` title check as `company_careers.py` before keeping a record. Not in `CONTEXT_FILTERED_SOURCES` for that reason. **The unfiltered feed is only the ~100 most-recently-posted jobs platform-wide**, not a paginated archive — expect a handful of analyst/data-titled matches per run (spot-checked: 2-4 per 100), accumulating across repeated runs as the feed rotates, not a bulk one-time yield.
- **`weworkremotely.py`** — remote-only job board, added 2026-09-09. Plain HTTP GET + RSS at `weworkremotely.com/remote-jobs.rss`, same reliability tier as `djinni.py`/`remoteok.py`. **The site's old per-category RSS URLs (`/categories/remote-...-jobs.rss`) no longer work** (301-redirect to the homepage, presumably a site redesign) — only the single unfiltered feed is live, with no "Data"/"Analytics" category of its own, so — like `remoteok.py` — this collector filters locally by title via `query_matches` and is not in `CONTEXT_FILTERED_SOURCES`. Better-structured than Djinni/RemoteOK's feeds though: `<region>`/`<country>`/`<state>` are separate fields, and the title itself is `"{Company}: {Job Title}"` — a clean split, not an HTML heuristic. Same small-window caveat as RemoteOK: only ~90-100 most-recent postings platform-wide, skewed toward eng/sales/management roles — a spot-check found zero analyst-titled matches among 90 items, so expect sporadic, accumulating-over-time yield, not a reliable per-run one. Investigated CryptoJobsList and Web3.career as crypto-domain-specific alternatives (matching the candidate's actual crypto/NFT background) — **neither has a usable feed**: CryptoJobsList's advertised RSS (`api.cryptojobslist.com/jobs.rss`) returns valid XML with zero `<item>` entries despite the site itself having active listings (confirmed 2026-09-09), and Web3.career advertises no RSS/feed at all. Both would need full HTML-scraping (LinkedIn/WTTJ-tier fragility) to add — not pursued, same reasoning as the work_ua/robota_ua sunk-cost note above; revisit only if the user explicitly wants it.
- **`work_ua.py`** / **`robota_ua.py`** — Ukraine's two largest *general* (not IT-only) job boards, added 2026-09-07 alongside Djinni because analyst roles at Ukrainian banks/retail/fintech (which value Excel/reporting over deep coding) show up here, not on IT-only boards — same rationale as Djinni: a native Ukrainian/Russian speaker has a real edge on this market regardless of physical location. Own query vocabulary in `job_queries.yaml`'s `ukrainian_categories` (free-text Ukrainian phrases, unlike Djinni's English category-filter labels). Neither has a public search API/RSS, so both are Playwright-rendered HTML (`page.goto`, not `get_with_retry`) with CSS-selector scraping, same fragility as `linkedin.py`/`wttj.py` — worse, in fact: **both are currently blocked by Cloudflare-style anti-bot challenges and are NOT in `--source all`.** `robota.ua` blocks consistently in both headless and non-headless mode, even with a fresh browser profile. `work.ua` is harder to reason about, not easier: one non-headless attempt succeeded, an identical repeat minutes later (same profile) was blocked — the profile/IP seems to get flagged after a handful of requests regardless of the headless flag. `is_challenge_page_title()` in each module turns a block into a visible error instead of a silent "0 results", but neither source is dependable today. Still selectable via `--source work_ua` / `--source robota_ua` for anyone who wants to retry (different IP, proxy, cooldown period, etc.) — kept rather than deleted since the scraping logic itself (selectors, pagination, salary/work-format parsing) is verified correct against real pages, only the anti-bot layer blocks it. **Sunk-cost trap, flagged 2026-09-08: don't spend further session time trying to fix the anti-bot blocking itself** (proxies, fingerprint tuning, header/UA tricks, etc.) — the 2026-09-07 stealth-context experiment already showed why this doesn't pay off: it bought exactly 3 clean requests before the same profile got flagged again, so each retry burns effort for a result that doesn't hold. Only revisit if the user explicitly asks, or an external condition changes (a proxy/IP the user already has, a long unattended cooldown, or the sites' own protection breaking, as already observed happening to robota.ua independent of anything this repo does).

**`core/ids.py` / `core/deduplication.py`** — identity is `source + normalized(url)` (tracking params and trailing slash stripped), falling back to `source + company + title + city_region` when there's no URL. `job_id = {source_slug}_{sha1[:12]}`. This makes reruns idempotent.

**`core/storage.py`** — `load_records`/`save_records`/`merge_records`. Raw XLSX (one per run, timestamped in `data/raw/`) has a single `raw_jobs` sheet plus CSV (UTF-8 with BOM). Processed XLSX (`data/processed/crypto_jobs_clean_v1.xlsx`, accumulative) has `jobs_master` (full canonical table), `jobs_view` (compact daily-use view), `jobs_text` (long descriptions/links), and `manual_review` (hand-filled fit assessment, keyed by `job_id` and preserved across reruns). Merge classifies each record as `new`/`existing`/`updated`.

**`core/analyzer.py`** (driven by `scripts/analyzer.py`) — rule-based, no ML/LLM. Infers `role_family`/`role_subcategory`, estimates `seniority` from title/text heuristics, extracts normalized `skills_all`, and splits `required_skills`/`preferred_skills` using known marker phrases. Everything it writes is explicitly preliminary (`analysis_note`) — title-based seniority guesses are known to be unreliable, and skills regex has false positives on short tokens (`R`) and generic terms (`ML`, `crypto`, `market data`).

**`core/metadata.py`** — salary/country/work-format extraction helpers shared across collectors and the availability checker.

**`scripts/active_near_fit_report.py`** — same manual_review + jobs_master join shape as skills_gap_report.py, but simpler: filters to postings that are both near-fit (`Подходит`/`Возможно`, not filtered) and still `active` per the last `--check-availability` run, and writes an `active_near_fit` sheet (decision-colored rows, same green/yellow convention as `manual_review`) as a ready-to-work applications list. Point-in-time — re-run after `--check-availability` to refresh, doesn't check liveness itself.

**`core/skills_gap.py`** (driven by `scripts/skills_gap_report.py`) — joins `jobs_master` (analyzer output: `required_skills`/`preferred_skills`) with `manual_review` (`decision`, `should_be_filtered`, `main_reason`, `review_notes`) by `job_id` to answer "what's costing me otherwise-plausible roles": which skills recur in near-fit (`Подходит`/`Возможно`) postings but aren't in the candidate's known toolset (`KNOWN_SKILLS`, sourced from `CV/resume_improved.html`'s own Skills section — update by hand if the resume changes), and which rejection-reason bucket (keyword-matched against `main_reason`+`review_notes`) accounts for the most `Не подходит`/`should_be_filtered=Да` postings. Buckets are split into learnable (coding depth, domain expertise, seniority, experience years — worth studying) vs structural (language, location/on-site, low-quality listing — not fixable by studying). Writes a `skills_gap` sheet into the processed workbook alongside printing to console.

**`core/skill_recommendations.py`** (also driven by `scripts/skills_gap_report.py`, added 2026-09-25) — turns the skill-gap counts into study recommendations backed by market data. For every missing or partially known skill it adds what share of ALL collected postings (deduped by normalized company+title, not only the hand-picked near-fit ones) mention the skill, using the analyzer's own `SKILL_RULES` patterns, and how many postings put a certification word within 60 characters of it. The action is computed from thresholds, never from a hardcoded tool name: `learn` (>=10% of the market and >=2 near-fit postings), `optional` (>=3%), `low priority`; skills in `LOW_CONFIDENCE_SKILLS` (`R`, `Machine Learning`, whose patterns match short or generic tokens) are downgraded to `verify first`. A certificate is called `worth considering` only if >=3 postings mention one, otherwise the advice is to learn the skill and skip the exam. Prints a "What to study" section and writes a `skill_recommendations` sheet (rows with action `learn` green). Known limit: interchangeable tools (Power BI / Tableau / Looker) are rated independently, so several can read `learn` even though learning one covers most of the demand. The market counts are mentions anywhere in the posting, a looser definition than the analyzer's required/preferred split, so they read higher than the near-fit counts by design.

**`scripts/certification_analysis.py`** — the market-side counterpart to `skills_gap`: counts, per posting (one mention per term, postings duplicated by normalized company+title collapsed first), how many collected postings mention each tool (SQL, Python, Excel, Power BI, Tableau, Looker, GA4, dbt, BigQuery, Snowflake, Dune, Metabase) and each certification (PL-300, Tableau, Google/IBM, GA4, CompTIA Data+, GitHub Foundations, dbt, plus a generic "certification near a tool name"), split by seniority and location (remote / Europe / other). Certificate rows also get required / preferred / merely-mentioned, decided by the nearest marker within +-150 characters. Term patterns live in `TOOLS`/`CERTIFICATES`; two are deliberately non-obvious — bare "Excel" is skipped when it is the English verb ("excel in", "to excel"), and bare "Dune" only counts capitalized. Read-only over `jobs_master`; writes `reports/certifications.csv` and `reports/certifications.md` (top-10 terms, certificate table, 3 random quotes per term to eyeball false positives). `reports/` is gitignored because it is derived from the real dataset and regenerable. Refuses to run (asks instead of guessing) if required columns are missing or fewer than 50 postings remain after dedup. The script only reports demand; study recommendations built from it (certificates, tools to learn) are personal and live outside the code.

**`scripts/recheck_rejected_boards.py`** — small standalone script (plan item B1 from the Notion-research backlog), added 2026-09-10. Re-runs the same live Greenhouse/Lever API check `collectors/company_careers.py` uses against `REJECTED_CANDIDATES`, a short hardcoded list of boards previously confirmed to exist but have zero open jobs (currently Kraken, Prodly — see the "checked 2026-09-05"/"checked 2026-09-09" comments in `config/companies.yaml`). Reports whether any now have postings, so a board starting to hire doesn't require re-mining the whole source list by hand — does not touch `career_boards` itself, adding a newly-active board is still a manual decision (relevance check first).

**`scripts/mine_notion_companies.py`** — standalone Playwright script (plan item B2 from the Notion-research backlog), added 2026-09-10. Extracts every row of a Notion-embed table view (e.g. `https://agilefluent.notion.site/600-...`, the 599-company "Russian-speaking roots" directory mined earlier for `career_boards` candidates) into a CSV, by clicking "Load more" in a loop and re-querying the button fresh from the live DOM before every click — this is what the earlier interactive-browser-tool attempt couldn't do reliably (its cached element `ref` kept resolving to a stale screen position once the DOM grew). Row/column extraction (`.notion-table-view-row` / `[data-col-index]` / `.notion-table-view-header-cell` selectors) is generic to any Notion-embed table, not tied to this one page. Pagination/extraction logic itself is covered by tests (`tests/test_mine_notion_companies.py`, including a real-headless-browser integration test against a synthetic Notion-like DOM) and is confirmed correct.
**Known issue, confirmed harder than expected: `agilefluent.notion.site` is Cloudflare-protected and rate-limits automated page loads (HTTP 429) more aggressively than a normal retry-with-backoff can work around.** Across two separate sessions and roughly a dozen total load attempts (script runs plus earlier interactive-browser-tool use against the same URL), only a couple got past the block, and once actually past it the table view itself sometimes still rendered "No results" for several seconds before the client-side app finished hydrating — so a bare `wait_for_selector` on `.notion-table-view-row` isn't sufficient evidence of a block either way. Diagnostic network capture (`page.on("response")`) showed the 429 is on the page's own document response, alongside a `challenges.cloudflare.com` request — this is the same class of protection as `work_ua`/`robota_ua`'s Cloudflare challenge (see that Resolved-adjacent note above), not a simple per-request throttle. **Treat this the same way: don't spend further session time retrying blind** — each attempt (including diagnostic ones) adds to the same rate-limit window instead of clearing it. A full 599-row dump likely needs a real cooldown (tens of minutes with zero requests to the domain, not just this script's own 4-attempt backoff) or a different network path (different IP/proxy) — revisit only if the user explicitly asks after enough real time has passed, or wants to run it from their own machine outside this environment.

**`core/fit_score.py`** (driven by `scripts/fit_score.py`) — plan item 8.1, added 2026-09-11: an LLM-based fit_score (0-100) plus a one-to-two-sentence `fit_reasoning`, both new `JobRecord` fields, computed by a single Anthropic API call per posting (`client.messages.create`, model `DEFAULT_MODEL = "claude-haiku-4-5-20251001"` — deliberately the cheap/fast tier since batch-scoring is a classification task, not deep reasoning; revisit if this grows into full CV drafting per item 8.2). The system prompt explicitly asks for a skeptical, non-inflating score and a strict JSON reply (`{"fit_score": int, "fit_reasoning": str}`), parsed tolerantly of stray code fences. **Deliberately not part of the free collect/analyze pass** — every call is billed, so it only runs from the explicitly-invoked `scripts/fit_score.py`, never automatically from `scripts/collector.py`/`scripts/analyzer.py`. `fit_score`/`fit_reasoning` are added to `core/storage.py:ANALYSIS_FIELDS` so they carry forward on re-collect the same way `role_family`/`skills_all` already do (see the `merge_records` fix above) — a re-scrape must never silently wipe a paid-for score.
`scripts/fit_score.py` reads `ANTHROPIC_API_KEY` from the environment (fails fast with a clear message if unset, rather than a raw SDK stack trace) and the master CV from `CV/resume_improved.html` (same file `core/skills_gap.py`'s `KNOWN_SKILLS` is sourced from — the project's one definition of "the candidate's real background"), extracted to plain text via BeautifulSoup. **Cost control, by default**: only scores postings with no `decision` yet in `manual_review` (a human has already judged decided ones — rescoring them would just be a second, worse-informed opinion) and skips postings that already have a `fit_score` unless `--rescore` is passed, so a rerun only pays for genuinely new postings. `--limit` (default 20) caps API calls per run regardless. One failed call (bad JSON, network error, rate limit) is logged and skipped, not fatal to the whole batch. `--model` lets a stronger model be substituted per-run without a code change. **Confirmed working against real data 2026-09-11**: first live run scored 8 postings, 0 errors — reasoning was concrete (US work-authorization requirements, specific tool-stack gaps like Cognos/Snowflake/Hadoop, domain mismatches like iGaming/FP&A) and matched the manual calibration patterns already established in this project.

**`core/cv_draft.py`** (driven by `scripts/cv_draft.py`) — plan item 8.2, added 2026-09-14: drafts 3-5 adapted CV bullets plus a short first-person "pitch" paragraph per posting, via one Anthropic API call (`DEFAULT_MODEL = "claude-sonnet-5"` — a stronger tier than `fit_score.py`'s Haiku is deliberate: this is generation a human recruiter reads, not classification, and volume is much lower since scope is narrow). **Deliberately not a formal cover letter** — the candidate's own existing hand-written drafts (`CV/application_drafts_round*.md`) are short, dry, factual first-person paragraphs naming concrete tools/employers and relocation stance, and the system prompt is built to match that established style, not invent a new one (see `resume-writing-style` memory: plain concrete verbs over qualifiers implying mastery, cut rather than pad). **Honesty gate is the system prompt's first rule, non-negotiable**: only rephrase/reorder/re-emphasize what's already in the CV, never invent an employer, tool, skill, degree, or achievement — this is the "8.2 must not fabricate experience" concern the roadmap's `gate` tag flagged before any code existed.
Output is one markdown file per posting at `CV/drafts/<job_id>.md` (title, company, location, the pitch as a blockquote, the bullets, a footer noting it's LLM-generated and needs review before sending) — **not** new `JobRecord`/spreadsheet columns like `fit_score`'s, since multi-paragraph generated text doesn't belong in a spreadsheet cell and the existing hand-written-draft convention is already file-based. **Scope, by design**: only postings marked `Подходит` (not `Возможно` — the strongest tier only, to keep the paid call volume small) AND still `availability_status == "active"` per the last `--check-availability` run — the same "worth acting on now" set `active_near_fit_report.py` surfaces to, filtered further. A rerun skips any posting that already has a draft file unless `--regenerate` is passed. `--limit` (default 10) caps calls per run; one failed call is logged and skipped, not fatal to the batch.
**Confirmed working against real data 2026-09-14** after two fixes found live: (1) `generate_draft` used to raise a bare, unexplained `JSONDecodeError` on a parse failure — it now catches that and re-raises with the response's `stop_reason` and content block types, which is what surfaced fix (2): `claude-sonnet-5` spends part of the same `max_tokens` budget on an internal "thinking" block before writing the JSON reply, and the original `700` consistently starved the real output (one posting got a truncated JSON body, another got zero text at all). Raised to `4096`. Also fixed a cosmetic bug in `render_draft_markdown`'s location line: postings with a blank `city_region` (common on Djinni, which often only gives a country) rendered a stray leading `" (Ukraine)"` — now joins only the non-empty parts.

**A real honesty-gate failure was found and fixed on the first full 16-posting batch**: 4 of 16 (all Ukraine-based Djinni postings) invented location claims not in the CV — `"based in Ukraine time zone"`, `"I'm based in Ukraine originally"` — despite the CV header stating the candidate's actual current city. The model was blurring past-employer location (two past employers, both in Ukraine) with current residence. Fixed with an explicit `LOCATION` rule in `SYSTEM_PROMPT`: never claim/imply physical presence anywhere but the CV's stated current location; a past employer's country is work history, not a residence claim. **Nuance, corrected once by the user**: a genuine timezone-overlap fact (the candidate's actual city is really only ~1h from Ukraine's timezone, worth mentioning for a remote posting) is fine to state — the first version of the fix banned all Ukraine-timezone phrasing, which was too broad; only the false physical-presence claim is the actual problem. Two tests guard both halves. **The 4 affected drafts predate this fix and should be regenerated** (`--regenerate` on those specific `job_id`s, or a full `--regenerate` pass) before being used.

The other 12/16 read well on manual review: dry, factual, tied to real CV lines, and one (Novoplex, iGaming) explicitly admitted a real skill gap (`"I don't have direct iGaming or formal A/B-testing experience"`) rather than papering over it. All 16 were later regenerated with the location fix; re-checked by hand and confirmed clean (timezone-overlap phrasing kept, no more false residency claims).

**`core/cv_review.py`** (driven by `scripts/cv_review.py`) — plan item 8.3, added 2026-09-14: reviews an already-written draft (`CV/drafts/<job_id>.md`, exactly as it is on disk, hand edits included) via one Anthropic API call — a genuinely separate, stateless call from the one that drafted it, not the same context "checking its own work". Same model tier and `MAX_OUTPUT_TOKENS=4096` as `cv_draft.py`, for the same reason (thinking-block budget). Checks four things: fabrication (every fact traces to the real CV), location honesty (mirrors `cv_draft.py`'s `LOCATION` rule — timezone-overlap facts are fine, residency claims elsewhere aren't), relevance (does it actually address the posting's requirements), and tone (dry/factual vs "success story"). Returns `{"verdict": "approved"|"needs_revision", "issues": [...]}`, appended to the draft file as a `## Ревью (агент-проверяющий)` section (`core/cv_review.py:append_review_section`) rather than a separate spreadsheet/file — keeps the whole application history for one posting in one readable place. Idempotent the same way as the other 8.x scripts: skips files that already have a review section unless `--regenerate`, which first strips the old section (`strip_existing_review`) so re-reviews replace rather than stack.
**Confirmed working against real data 2026-09-14, found one systematic false positive on the first 16-draft batch**: 15/16 came back `needs_revision`, and nearly all of them cited the same thing — the draft file's own header line (e.g. `"Ukraine · Remote"`, rendered by `cv_draft.py:render_draft_markdown` from the *posting's* location/work-format) mistaken for a candidate residency claim, flagged even on drafts whose pitch correctly stated the candidate's actual current city right below it. The reviewer was never told that line is the vacancy's own metadata, not the candidate's words. Fixed with an explicit clarification in `SYSTEM_PROMPT` (the header line is the posting's, only the blockquoted pitch/bullets are the candidate's own claims to check). The other issues raised in that same batch were genuine and worth keeping in mind for future prompt tuning: legitimate wording-drift catches (a CV's "concrete findings" reworded to "concrete recommendations" — a small but real overreach) and at least one real, useful content gap (a posting requiring an in-progress degree while the CV shows one completed in 2022, unaddressed in the draft) — confirms the reviewer's actual judgment is sound once the location-line confusion is removed as noise. After a second `--regenerate` pass, 3/16 approved cleanly; the reviewer's remaining findings across the batch surfaced two postings with real structural mismatches beyond a wording fix (Binance BAP — an internship requiring an in-progress degree; FERM — needs specific CRM/client-database experience the candidate doesn't have) plus WhiteBIT (wants 5+ years product analytics on a specific stack) — all three recalibrated to `Не подходит` in `manual_review` rather than left as `Подходит`. The other 9 flagged drafts got hand-corrected wording (mainly a recurring real fabrication: several drafts attributed Yandex Metrica/LiveInternet to one past employer when the CV only credits the other one with those tools) with a `**Правки внесены вручную**` note appended to each file.

**`core/pdf_assembly.py`** (driven by `scripts/cv_pdf.py`) — plan item 8.4, added 2026-09-14: assembles a one-page cover-note PDF per draft (title/company header, the pitch, the CV bullets) via Playwright `page.pdf()` — no new rendering dependency, Playwright was already required for the collectors. **No LLM call and no `ANTHROPIC_API_KEY` needed** — 8.4 is purely mechanical, unlike 8.1-8.3: `extract_draft_content` re-parses the pitch/bullets straight out of the (possibly hand-edited) draft markdown via regex, and the ATS keyword check reuses `record.required_skills`/`preferred_skills` (the rule-based analyzer's own output) rather than asking a model to name keywords again. `pypdf` (new, lightweight, pure-Python dependency) reads the *rendered PDF's actual text layer* back out — deliberately not the source HTML/markdown — since the point of an "ATS check" is to catch the real, mundane failure mode where a PDF looks fine visually but its text layer is garbled/unselectable (bad font embedding, ligatures), which only shows up by reading the PDF itself. Output is `CV/drafts/<job_id>.pdf` plus an appended `## ATS-проверка текстового слоя` section in the same `.md` file (matched/missing keywords, and whether the text layer extracted at all) — same one-file-per-posting convention as the reviewer. Idempotent: skips a draft that already has both a `.pdf` and an ATS section unless `--regenerate`.
**Confirmed working on all 16 live drafts (2026-09-14), one interesting real-world finding, not a bug**: the naive keyword check counts a skill as "matched" even when the pitch is explicitly *disclaiming* it (e.g. "I don't have direct Tableau/Snowflake/Airflow experience" registers all three as "found" in the PDF text) — this is left as-is because it faithfully mirrors how many real basic ATS keyword parsers actually behave (they're well known for not understanding negation), not a defect in this script. Worth keeping in mind when writing future pitches: naming a missing tool to be transparent about a gap can register as a false keyword hit on a naive parser, exactly the tradeoff a human has to weigh, which this tool surfaces rather than hides.

**`core/applications.py`** (driven by `scripts/applications_tracker.py`) — plan item 8.5, added 2026-09-15: the actual gate the roadmap called for. Turns every `CV/drafts/<job_id>.md` into one row in a new `applications` sheet (`job_id`, `title`/`company`/`url` looked up from `jobs_master`, `review_verdict` read back from the file via `core/cv_review.py:extract_verdict`, `pdf_ready` from whether the matching `.pdf` exists) with a human-editable `status` (`draft`/`approved`/`rejected`/`sent`, dropdown-validated, color-coded like `manual_review`'s decision fills) and free-text `notes`. **No LLM call, no network access** — purely local bookkeeping, same free tier as `cv_pdf.py`. `status`/`notes` (and the original `date_added`) are preserved across reruns the same way `manual_review`'s `MANUAL_INPUT_COLUMNS` are (`core/applications.py:build_applications_rows` merges against `load_existing_applications`'s prior sheet contents) — a rerun must never quietly reset a human's approval decision back to `draft`, while `review_verdict`/`pdf_ready` still refresh from the current file state every time. **Nothing in this pipeline sends anything** — moving a row to `approved` is a human decision recorded here, and `sent` is only set after the human has actually sent it themselves outside this tool.

**`--check-availability`** (in `scripts/collector.py`) re-verifies saved `url`s: LinkedIn/WTTJ via browser + source-specific closed-page markers, Company Careers via a live Greenhouse/Lever API lookup (matched by URL or Greenhouse `gh_jid`), everything else via a generic HTTP status/body-text detector (`detect_generic_availability`). Writes `active`/`closed`/`unknown` into `availability_status`.

## Known gaps (don't "fix" without checking with the user first — several are tracked, in-progress work)

- `tmpghebr_w6/` at repo root is an empty leftover directory, not part of the architecture.

## Resolved (kept here so a future session doesn't re-"discover" and re-fix these)

- **`merge_records` used to silently erase analyzer output on every re-collect** (`core/storage.py`) — a re-scraped, not-yet-analyzed record would fully replace the analyzed one on merge, wiping `role_family`/`skills_all`/`availability_status`. Fixed: analysis/availability now carry forward from the previous record when the incoming one doesn't have them.
- **Retry/backoff is wired in** via `collectors/base.py:get_with_retry`, used by every plain-HTTP-GET collector (`company_careers.py`, `djinni.py`, `remoteok.py`) and the `--check-availability` ATS lookup — reads `retry_attempts`/`delay_max_seconds` from `settings.yaml`, which were configured but unused before.
- **Persistent file logging exists**: `core/logging.py:write_run_log` appends one JSON-lines record per run (source stats, merge counts, errors) to `logs/collector.jsonl`, called from both `run_collection` and `run_availability_check`.
- **`core/analyzer.py` now recognizes Ukrainian/Russian role/seniority phrasing** (Djinni.co postings) — before this, every Djinni record got `role_family="Other"`, `seniority="Unknown"` regardless of content.
- **Hybrid/On-site postings anchored to a disallowed country are now filtered automatically** — `core/metadata.py:requires_onsite_in_disallowed_country`, config `exclusion.disallowed_onsite_countries` in `job_queries.yaml` (currently `Ukraine`, `India`), wired into `scripts/collector.py:run_collection` alongside the other exclusion checks. Before this, three real cases (Novoplex/Poland, Coinbase/Hyderabad-India, BRDO/Kyiv-Ukraine) all had to be caught by hand in `manual_review`. A Remote posting nominally based in a disallowed country is unaffected — only Hybrid/On-site (physical presence) is checked. This is separate from `disallowed_work_formats` (currently empty — Hybrid/On-site is fine in general, just not in these specific countries).
- **Confirmed Greenhouse has no Lever-equivalent structured work-format field** (2026-09-08) — queried `job.metadata[]` across all 8 currently configured Greenhouse boards (`coinbase`, `drweng`, `gsrmarkets`, `bybit`, `okx`, `falconx`, `nansen`, `flowtraders`): every board's `metadata` is recruiter/ATS-internal fields (`Team`, `Employment Type`, `Division`, internal IDs), nothing about office/remote/hybrid. `job.location.name` is the only structural signal, and it's free text with no fixed format (`"Remote - Cyprus"`, `"Hybrid - Bangalore, India"`, `"Chicago, IL"`, `"APAC - Remote; Dubai, UAE"`) — `collectors/company_careers.py:_greenhouse_record` already feeds it into `infer_work_format` alongside the description, so explicit `Remote`/`Hybrid` mentions in it are already caught. Not caught: a bare office name with no qualifier (e.g. `"Chicago, IL"`) almost always means on-site in Greenhouse's convention, but inferring that risks misreading region-only labels (bare `"APAC"`/`"EMEA"`) as a specific office — a judgment call left to the user, not implemented.
- **Fixed `extract_salary` not recognizing comma as a thousands separator** (2026-09-08) — `core/metadata.py:extract_salary`; found while writing the `_greenhouse_record` fixture test. `"$80,000 - $95,000"` used to cut short at `"$80,00"` because comma was only wired in as a *decimal* separator (1-2 digits), never a thousands one (3 digits) — only space/dot thousands separators worked before (`"80 000"`/`"80.000"`), so the common US comma-thousands format was silently mangled. Fix: the thousands group now also accepts `,`; no ambiguity with the decimal group since thousands requires exactly 3 digits after the separator and decimal requires 1-2, so `"45,000"` and `"45,50"` still resolve to their respective correct reading.
- **Added real browser-integration tests for LinkedIn/WTTJ `record_from_page` and WTTJ pagination** (2026-09-08) — both had zero test coverage since they're built from a live Playwright `Page`/drive a full `collect()` loop, not a plain JSON dict a unit test can fake directly. `tests/test_linkedin_collector.py`/`tests/test_wttj_collector.py` now launch a real headless Chromium (already present in the venv), render `tests/fixtures/*.html` via `page.set_content()`, and run the actual `record_from_page` against that DOM (covers both the JSON-LD path and the CSS-selector fallback path) — this exercises the actual locator/selector logic (`first_text`, `json_ld_job`), not just its string-level description. WTTJ pagination is tested by using `context.route()` to intercept navigation to `welcometothejungle.com` and serve synthetic paginated listing/detail pages, so the real `WttjCollector.collect()` loop runs end-to-end offline and deterministically — confirms it stops at the correct page for a given `limit` and walks multiple pages when the limit allows. Adds ~2s to the test run (125 tests, ~3.5s total) — the cost of a real browser launch, judged acceptable.
- **German-language exclusion filter added** (2026-09-08) — `core/metadata.py:requires_german`, mirrors `requires_french` exactly (explicit `german_phrases` incl. the `m/w/d`/`w/m/d`/`d/m/w`/`m/w/x` gender-neutral marker equivalent to French `h/f`, plus `german_word_markers` density check), config in `job_queries.yaml`'s `exclusion.german_phrases`/`german_word_markers`/`german_word_marker_threshold`, wired into `scripts/collector.py:run_collection` alongside `requires_french`.
- **`save_records` used to silently wipe any sheet it doesn't itself write** (`core/storage.py`) — pandas `ExcelWriter`'s default mode rebuilds the whole workbook from scratch, so `skills_gap_report.py`'s `skills_gap` sheet got deleted on every later `analyzer.py` run or `--check-availability` pass (confirmed 2026-09-07: a `--check-availability` run wiped a freshly generated `skills_gap` sheet). Fixed: `save_records` now opens with `mode="a", if_sheet_exists="replace"` whenever the output file already exists, which preserves untouched sheets while still fully replacing `jobs_master`/`jobs_view`/`jobs_text`/`manual_review`.
- **Personal-fit summary, city/region split, and salary USD normalization added** (2026-09-11 — plan items 7.2/7.3 from the project roadmap). Three separate, independent additions:
  - **`personal_fit`** — a new `manual_review` column (`core/storage.py:compute_personal_fit`), positioned right before `decision`. Purely a *display fold* of the two existing manual columns (`"Не подходит — <main_reason>"`, or just the decision when it's `Подходит` or `main_reason` is blank) — not a new judgment call. Recomputed fresh from the current `decision`/`main_reason` on every `save_records` call (it's excluded from `MANUAL_INPUT_COLUMNS`, so nothing gets "preserved" stale). This is deliberately *not* the auto-computed fit score the old "Known gaps" note warned against building — that would have re-derived a judgment with lower confidence than the human's; this only reformats what the human already decided.
  - **`city`/`region`** — split out from the existing free-text `city_region` by `core/metadata.py:split_city_region`, a heuristic comma-split (drops a trailing segment that duplicates `country`, first remaining segment is `city`, the rest joins into `region`) — not a geocoder, since `city_region`'s shape varies per source (WTTJ: "Paris, Ile-de-France, FR"; RemoteOK: 3-4 comma-joined segments; Djinni: often just a bare country name). Computed in `core/analyzer.py:analyze_record` (same rule-based pass as everything else derived, not at collector parse time) and added to `jobs_view`.
  - **`salary_usd_equivalent`** — rough USD conversion of the existing free-text `salary` string, via `core/metadata.py:normalize_salary_usd`. Uses a small **static, hand-maintained FX rate table** (`_USD_FX_RATES`, fixed at 2026-09 rates) rather than a live FX API call — a deliberate choice to avoid adding a network dependency (and its own failure modes, e.g. the Notion script's 429 issue above) to the analyzer pass; ask before switching to a live rate source, since that changes the accuracy/dependency tradeoff. Returns `""` when the salary is already USD or the currency isn't recognized — it's a rough cross-posting comparison aid, not a figure to make decisions on. Computed in the same `analyze_record` pass, added to `jobs_view`.
- **`load_manual_review` used to leak pandas' NaN-for-a-blank-cell as the literal string `"nan"`** (`core/storage.py`), found 2026-09-11 the first time a fresh `--source all` + `analyzer.py` run produced brand-new, never-reviewed `manual_review` rows and `scripts/fit_score.py --limit 5` reported `0` candidates instead of the 8 that were actually sitting there undecided. Root cause: `pd.read_excel` reads an untouched blank cell as float `nan`, and `str(nan)` is the literal text `"nan"` — truthy and non-empty. Two consumers broke silently on this: `compute_personal_fit` folded it into a `"nan — nan"` personal_fit for every not-yet-reviewed posting (visible in the sheet, easy to miss since a fully-calibrated dataset never has any blank rows to trigger it), and `scripts/fit_score.py`'s `select_candidates` treated `"nan"` as a real `decision` and silently skipped scoring every brand-new posting — the exact case the whole cost-control design was built for. Fixed: `load_manual_review` now normalizes every NaN float to `""` before returning. Regression test: `tests/test_storage_and_logging.py:test_load_manual_review_returns_empty_string_not_nan_for_blank_cells` (saves twice — the bug only shows up on the *second* save, once blank cells have round-tripped through disk).
